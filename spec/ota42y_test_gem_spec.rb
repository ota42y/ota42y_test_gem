require "spec_helper"

RSpec.describe Ota42yTestGem do
  it "has a version number" do
    expect(Ota42yTestGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
